import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pagamento profissional",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
